# 佧了提审整改执行记录

目标：取得可复核的提审证据；本轮不提交微信审核、不发布、不付款，不承诺零缺陷。

## 基线与保护

- 基线：远端 main `f52c71a8d0073d1fc0f27a524cb7d0aaefef4a26`，与原 operational-hardening 工作树内容一致。
- 新分支：`codex/review-readiness`；独立工作树 `.worktrees/review-readiness`。
- 保留全部旧工作树、未提交修改及根目录未跟踪的 `图标.png`。
- 保留 10 位学号规则，不采集真实教职工工号，不接入手机号。

## 执行顺序

1. [进行中] 新鲜依赖审计，兼容修复；无法消除的风险逐条审查可达性和缓解措施，不仅延期。
2. [进行中] 客户端及云端只读行为审查，整理可复现缺陷与回归测试。
3. [待执行] 对确认缺陷先补失败测试、再修复；依次进行规格及质量复核。
4. [待执行] 按真实数据处理完善隐私说明、后台逐字段清单及保存期限。
5. [待执行] 将代码检查与完整提审证据门禁分离；缺证据默认不通过。
6. [待执行] 修改后统一版本 0.6.1；执行全量本地检查与双平台 CI。
7. [等待授权] CloudBase 登录后核对套餐、权限、索引、函数、触发器及任务状态；应用精确存储规则并回读。套餐拒绝则停止相关部署并列阻塞。
8. [待执行] PR、CI、合并、受影响函数部署及开发版上传，不重复无关数据迁移。
9. [等待用户证据] 最终开发版三角色受影响流程复测；后台备案、类目、隐私生效、模板及密钥轮换状态确认。
10. [待执行] 输出提交号、版本、检查时间、证据和阻塞清单；全部满足才标记可提审。

## 当前已知证据缺口

- 原依赖例外已于 2026-08-26 过期；正在重新审计，不视为通过。
- CloudBase 凭据失效；新授权等待完成，历史回读不能代替本轮检查。
- 用户截图显示备案通过、仅 0.6.0 开发版；不等于隐私指引已经生效或版本已审核。
- 既往三账号验收为用户已确认事项；不能自动沿用为后续修改版本的验收。
- 旧存储规则回读未达到精确契约，必须重新验证。

任何缺少证据的项目保持“未验证”，不得使用占位成功值通过门禁。
